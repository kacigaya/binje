import Feather from '@expo/vector-icons/Feather'; import { router } from 'expo-router'; import { Pressable,StyleSheet } from 'react-native'; import { useSafeAreaInsets } from 'react-native-safe-area-context'; import { useLocale } from '../providers/LocaleProvider'; import { colors } from '../theme';
function goBack(){if(router.canGoBack())router.back();else router.replace('/' as never)}
export function BackButton({floating=true}:{floating?:boolean}){const {t}=useLocale();const insets=useSafeAreaInsets();return <Pressable accessibilityRole="button" accessibilityLabel={t('back')} onPress={goBack} hitSlop={8} style={({pressed})=>[s.button,floating&&[s.floating,{top:insets.top+16}],pressed&&{opacity:.7}]}><Feather name="chevron-left" size={22} color={colors.text}/></Pressable>}
const s=StyleSheet.create({button:{width:36,height:36,borderRadius:18,backgroundColor:'rgba(0,0,0,0.6)',borderWidth:1,borderColor:'rgba(255,255,255,0.15)',alignItems:'center',justifyContent:'center'},floating:{position:'absolute',left:16,zIndex:10}});
