import { FlatList,StyleSheet,Text,View } from 'react-native'; import type { MobileMediaSummary } from '../types/api'; import { MediaCard } from './MediaCard'; import { colors,spacing } from '../theme';
export function MediaRow({title,items}:{title:string;items:MobileMediaSummary[]}){if(!items.length)return null;return <View style={s.section}><Text accessibilityRole="header" style={s.title}>{title}</Text><FlatList horizontal data={items} keyExtractor={i=>`${i.mediaType}-${i.id}`} renderItem={({item})=><MediaCard item={item}/>} contentContainerStyle={s.list} ItemSeparatorComponent={()=> <View style={{width:12}}/>} showsHorizontalScrollIndicator={false}/></View>}
const s=StyleSheet.create({section:{marginTop:spacing.lg},title:{color:colors.text,fontSize:21,fontWeight:'800',paddingHorizontal:spacing.md,marginBottom:spacing.md},list:{paddingHorizontal:spacing.md}});
